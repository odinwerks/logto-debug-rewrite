# Your Logto application ID
APP_ID=your-app-id

# Your Logto application secret
APP_SECRET=your-app-secret

# Your Logto endpoint (e.g., https://auth.yourdomain.org)
ENDPOINT=https://auth.yourdomain.org

# Your application's base URL
BASE_URL=http://localhost:3000

# Cookie secret for session encryption (generate a random string)
COOKIE_SECRET=your-random-cookie-secret

# Optional: Additional scopes (comma-separated)
# SCOPES=openid,profile,email,phone,custom_data,identities,organizations

# Theme Configuration
# Theme folder name (default: default)
# THEME=default
# NEXT_THEME=default

# Available themes (comma-separated)
# THEMES_AVAILABLE=default
# NEXT_THEMES_AVAILABLE=default

# Default theme mode before user preferences are read from Logto (dark | light)
# DEFAULT_THEME_MODE=dark
# NEXT_PUBLIC_DEFAULT_THEME_MODE=dark

# i18n Configuration
# Default language (en-US or ka-GE)
# LANG_MAIN=en-US
# NEXT_LANG_NAME=en-US

# Available languages (comma-separated)
# LANG_AVAILABLE=en-US,ka-GE
# NEXT_LANG_AVAILABLE=en-US,ka-GE

# Tab Configuration
# Which tabs to show and in what order (comma-separated)
# Allowed: profile, custom-data, identities, organizations, mfa, raw
# Aliases: personal, user → profile; custom, customdata → custom-data; identity → identities; orgs, org → organizations; 2fa, totp → mfa; debug, data → raw
# LOAD_TABS=profile,custom-data,mfa,organizations,identities,raw
# NEXT_PUBLIC_LOAD_TABS=profile,custom-data,mfa,organizations,identities,raw

# MFA Configuration
# Name that will show up in the TOTP QR.
# NEXT_PUBLIC_MFA_ISSUER=Nebakoploba

# Note: User preferences (theme and language) are automatically stored in Logto customData under the "Preferences" key.
# Update your Logto application's JSON schema to include this key for type safety.
